import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inline: {
    flexDirection: "row",
    marginBottom: 6,
  },
  likesNum: {
    color: "#0099ff",
    marginLeft: 5,
    marginTop: 2,
  },
});
