import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inline: {
    flexDirection: "row",
    marginBottom: 6,
  },
  descriptionText: {
    fontSize: 15,
    marginLeft: 5,
    color: "#0099ff",
  },
});
